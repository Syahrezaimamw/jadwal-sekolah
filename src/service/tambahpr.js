import { useEffect, useState } from "react"

export const tambahpr=(dataa,perr,idd)=>{
    const data={
        data:dataa,
        per:perr,
        id:idd
    }
    // console.log(data)
    let da='aa'
    return da
    // const [data,setData]=useState()
  
}